using UnityEngine;
using System.Collections;

public class ForGameScript : MonoBehaviour {

   /* public GameObject gameScript;// = new Texture2D(10, 10);
    // public menu;
    bool paused = false;
    //  public Camera camera = new Camera();
    //public AudioClip music = new GameObject();
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.F2))
            if (!paused)
            {


                Time.timeScale = 0;
                paused = true;

                menu1.SetActive(true);
                //menu.



            }
            else
            {


                Time.timeScale = 1;
                paused = false;


                menu1.SetActive(false);
            }*/
}
